# Article Visualizer

Minimal Next.js app with a single full-page React component.

## Quick Start

Install dependencies and start the dev server:

```bash
bun install
bun run dev
```

Edit `app/page.tsx` to swap in your own component.
